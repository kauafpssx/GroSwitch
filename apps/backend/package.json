{
  "name": "@groswitch/backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/server.ts",
    "build": "bun build src/server.ts --outdir dist --target bun",
    "start": "bun run dist/server.js",
    "db:push": "prisma db push",
    "db:studio": "prisma studio",
    "db:generate": "prisma generate"
  },
  "dependencies": {
    "@fastify/cors": "^11.0.0",
    "@groswitch/common": "workspace:*",
    "@prisma/client": "^6.3.0",
    "dotenv": "^16.4.7",
    "fastify": "^5.2.1",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@types/bun": "^1.2.3",
    "@types/js-yaml": "^4.0.9",
    "pino-pretty": "^13.1.3",
    "prisma": "^6.3.0",
    "typescript": "^5.7.3"
  }
}
